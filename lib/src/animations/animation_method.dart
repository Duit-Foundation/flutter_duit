/// A property indicating which method will be called
/// on the controller when the animation is triggered
enum AnimationMethod {
  forward,
  repeat,
  reverse,
  /// Toggle between forward and reverse
  toggle,
}
