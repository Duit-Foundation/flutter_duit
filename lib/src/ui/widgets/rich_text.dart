import "package:flutter/material.dart";

class DuitRichText extends StatelessWidget {
  const DuitRichText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text.rich(TextSpan(text: "Hello "));
  }
}
