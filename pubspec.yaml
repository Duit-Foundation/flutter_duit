name: flutter_duit
description: Server driver UI framework for Flutter. Allows you to update your cool UI without updating the app!
version: 1.2.0
repository: https://github.com/lesleysin/flutter_duit
issue_tracker: https://github.com/lesleysin/flutter_duit/issues

environment:
  sdk: '>=3.1.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
