import { Color } from "../utils/color";

export interface TextStyle {
    color?: Color;
    fontFamily?: string;
    fontSize?: number;
    letterSpacing?: number;
    wordSpacing?: number;
    height?: number;
}